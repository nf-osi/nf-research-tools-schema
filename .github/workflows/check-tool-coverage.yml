name: Check Tool Coverage and Suggest Novel Tools

on:
  schedule:
    # Run weekly on Mondays at 9 AM UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:  # Allow manual triggering
    inputs:
      ai_validation:
        description: 'Run AI validation on mined tools using Goose'
        required: false
        type: boolean
        default: true
      max_publications:
        description: 'Maximum number of publications to mine (blank = all)'
        required: false
        type: string
        default: ''
      force_rereviews:
        description: 'Force re-review of already-reviewed publications'
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  issues: write

jobs:
  analyze-coverage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r tool_coverage/requirements.txt

      - name: Check for ANTHROPIC_API_KEY
        id: check_api_key
        run: |
          if [ -n "${{ secrets.ANTHROPIC_API_KEY }}" ]; then
            echo "api_key_exists=true" >> $GITHUB_OUTPUT
            echo "✅ ANTHROPIC_API_KEY is configured"
          else
            echo "api_key_exists=false" >> $GITHUB_OUTPUT
            echo "⚠️  ANTHROPIC_API_KEY not found - AI validation will be skipped"
          fi

      - name: Install Goose CLI
        if: ${{ github.event.inputs.ai_validation != 'false' && steps.check_api_key.outputs.api_key_exists == 'true' }}
        run: |
          echo "Installing Goose CLI for AI validation..."
          # Install Go if needed
          sudo apt-get update
          sudo apt-get install -y golang-go
          # Install Goose
          go install github.com/block/goose@latest
          # Add to PATH
          echo "$HOME/go/bin" >> $GITHUB_PATH

      - name: Configure Goose
        if: ${{ github.event.inputs.ai_validation != 'false' && steps.check_api_key.outputs.api_key_exists == 'true' }}
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Configuring Goose with Anthropic API..."
          # Goose will use ANTHROPIC_API_KEY from environment

      - name: Mine publications for novel tools
        id: mining
        env:
          SYNAPSE_AUTH_TOKEN: ${{ secrets.SYNAPSE_AUTH_TOKEN }}
        run: |
          echo "Mining publications for novel tools..."

          # Build command with optional flags
          CMD="python tool_coverage/scripts/fetch_fulltext_and_mine.py"

          # Add max publications limit if specified
          if [ -n "${{ github.event.inputs.max_publications }}" ]; then
            CMD="$CMD --max-publications ${{ github.event.inputs.max_publications }}"
          fi

          # Run mining
          echo "Running: $CMD"
          $CMD 2>&1 | tee mining_output.log || echo "mining_failed=true" >> $GITHUB_OUTPUT

          echo "mining_complete=true" >> $GITHUB_OUTPUT

      - name: Run AI validation on mined tools
        id: validation
        if: ${{ github.event.inputs.ai_validation != 'false' && steps.check_api_key.outputs.api_key_exists == 'true' }}
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Running AI validation on mined tools and candidate publications..."

          # Build command with optional flags
          # Note: --include-unlinked is now the default (fetches from NF portal + unlinked tools pubs)
          CMD="python tool_coverage/scripts/run_publication_reviews.py --mining-file processed_publications.csv"

          # Add force re-reviews flag if specified
          if [ "${{ github.event.inputs.force_rereviews }}" = "true" ]; then
            CMD="$CMD --force-rereviews"
          fi

          # Run validation
          echo "Running: $CMD"
          $CMD 2>&1 | tee validation_output.log || echo "validation_failed=true" >> $GITHUB_OUTPUT

          echo "validation_complete=true" >> $GITHUB_OUTPUT

      - name: Apply pattern improvements
        id: pattern_improvements
        if: ${{ github.event.inputs.ai_validation != 'false' && steps.check_api_key.outputs.api_key_exists == 'true' && steps.validation.outputs.validation_complete == 'true' }}
        run: |
          echo "Applying AI-suggested pattern improvements..."
          python tool_coverage/scripts/apply_pattern_suggestions.py 2>&1 | tee pattern_improvements_output.log

          # Check if any files were modified
          if [ -f "tool_coverage/config/mining_patterns.json" ] || [ -f "PATTERN_IMPROVEMENTS.md" ]; then
            echo "pattern_improvements_applied=true" >> $GITHUB_OUTPUT
          fi

      - name: Format mining results for submission
        id: formatting
        run: |
          echo "Formatting mining results into submission CSVs..."
          if [ -f "processed_publications.csv" ]; then
            python tool_coverage/scripts/format_mining_for_submission.py 2>&1 | tee formatting_output.log
            echo "formatting_complete=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Skipping formatting - no mining results found"
          fi

      - name: Analyze tool coverage
        id: coverage
        env:
          SYNAPSE_AUTH_TOKEN: ${{ secrets.SYNAPSE_AUTH_TOKEN }}
        run: |
          echo "Running coverage analysis with validated results..."
          python tool_coverage/scripts/analyze_missing_tools.py 2>&1 | tee coverage_output.log

          # Extract key metrics for summary
          echo "coverage_complete=true" >> $GITHUB_OUTPUT

      - name: Generate summary report
        id: summary
        env:
          SYNAPSE_AUTH_TOKEN: ${{ secrets.SYNAPSE_AUTH_TOKEN }}
        run: |
          python tool_coverage/scripts/generate_coverage_summary.py > pr_body.md

          # Read the summary for the PR body
          echo "PR_BODY<<EOF" >> $GITHUB_OUTPUT
          cat pr_body.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Upload reports as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: tool-coverage-reports
          path: |
            GFF_Tool_Coverage_Report.pdf
            gff_publications_MISSING_tools.csv
            processed_publications.csv
            priority_publications_FULLTEXT.csv
            GFF_publications_with_tools_FULLTEXT.csv
            SUBMIT_*.csv
            VALIDATED_*.csv
            tool_reviews/validation_report.xlsx
            tool_reviews/validation_summary.json
            tool_reviews/potentially_missed_tools.csv
            tool_reviews/suggested_patterns.csv
            tool_reviews/results/*.yaml
            tool_coverage/config/mining_patterns.json
            PATTERN_IMPROVEMENTS.md
            coverage_output.log
            mining_output.log
            validation_output.log
            formatting_output.log
            pattern_improvements_output.log
          retention-days: 90

      - name: Create Pull Request with results
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.NF_SERVICE_GIT_TOKEN }}
          commit-message: |
            Update tool coverage: automated mining results

            - Coverage report updated
            - Novel tools mined from publications
            - Ready for Synapse upload after review
          branch: tool-coverage-update-${{ github.run_number }}
          delete-branch: true
          title: '🔍 Tool Coverage Update - ${{ github.run_number }}'
          body-path: pr_body.md
          labels: |
            automated-mining
            tool-coverage
          add-paths: |
            VALIDATED_*.csv
            SUBMIT_*.csv
            GFF_Tool_Coverage_Report.pdf
            processed_publications.csv
            priority_publications_FULLTEXT.csv
            GFF_publications_with_tools_FULLTEXT.csv
            mining_summary_ALL_publications.csv
            gff_publications_MISSING_tools.csv
            tool_coverage/config/mining_patterns.json
            PATTERN_IMPROVEMENTS.md
