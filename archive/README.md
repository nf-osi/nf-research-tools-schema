# Archive

Historical files kept for reference, no longer the source of truth for anything live.

- `nf_research_tools.rdb.model.csv` — the flat CSV data model this repo used
  before the LinkML migration (see `docs/MIGRATION.md`). Superseded by the
  modular LinkML schema in `modules/`. Archived as part of Phase 7 of the
  migration (2026-08-24).
